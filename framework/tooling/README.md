# Tooling Module

## Overview

The `tooling` module provides comprehensive tool management capabilities for the WOLF framework. It enables discovery, documentation, execution, and semantic search of language-agnostic tools (Python, JavaScript, Go, Bash, Rust, etc.). Tools can be organized in ToolBoxes for efficient management and retrieval.

## Architecture

The tooling system consists of two main components that work together:

