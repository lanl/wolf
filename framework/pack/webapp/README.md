# WOLF Gateway Web UI

This directory contains the static web UI served by `./wolf gateway` / `framework.pack.gateway.WolfGateway`.

It intentionally lives under `framework/pack/` so gateway-specific UI assets are separate from the Wolf app GUI under `framework/gui/static/` and from the legacy temporary client under `framework/ui/webapp/`.
